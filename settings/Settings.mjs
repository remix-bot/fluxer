/**
 * @file Settings.mjs — Dashboard Settings — server-side settings manager for the web dashboard
 * @module settings.Settings
 */

/**
 * Re-exports from src/Settings.mjs for backward compatibility.
 * The canonical definitions now live in src/Settings.mjs.
 */
export { SettingsManager, ServerSettings, RemoteSettingsManager } from "../src/Settings.mjs";
