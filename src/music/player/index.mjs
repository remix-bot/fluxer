/**
 * @module src/music/player
 * @description Public surface of the per-channel player.
 *
 * Import map from the old layout:
 * - `src/Player.mjs` → this module (default export = Player)
 */

export { default, Player, Queue } from "./Player.mjs";
